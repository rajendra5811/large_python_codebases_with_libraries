def deposit(self ,amount): self.Balance += amount  return self.Balance   state change
def find_student(self, student_id):  # search  teacher- 
 for self.student in students:  if student_id == student.id: print(f'{student_id} is found')  else: return None
def add_teacher(self, teacher_id, teacher_name):  self.teachers.append(Teacher(teacher_id, teacher_name))  print(f'Teacher {teacher_name} added successfully!') 
def remove_employee(self, employee_id, employee_name):  self.employees.remove(Employee(employee_id, employee_name)) print(f'Employee {employee_name} removed successfully!')
available = []
def available_rooms(self): rooms = Hotel.available for rooms in available:  print(rooms) else:  Return None 
def display_book (self): print(f"{self.book.id}\t{self.book.title} {self.book.author}")
def change_pin(self, new_pin): self.pin = new_pin  print("Pin changed successfully!")
def  transfer(self, sender, recevier, amount): if amount<0: raise Value_Error() if sender.status == 'active'and recevier.status =='active': deposit(self,sender,amount) withdraw(self, reciever) print("transfer successfully!") 
class ElectricCar(Car):
     def __init__(self, mileage, price, life_in_years): 
        super()

